import { ISection } from 'app/components/section-table/types';
import { IStatusFilterOption } from './types';
export declare const updateSectionsWithFilterAndSearchResult: (sections: Array<ISection>, searchTerm?: string, option?: IStatusFilterOption) => Array<ISection>;
